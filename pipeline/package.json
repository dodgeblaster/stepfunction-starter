{
    "scripts": {
        "deploy": "aws cloudformation deploy --stack-name=StepFunctionStarterPipeline --template-file=template.yml --capabilities=CAPABILITY_IAM",
        "remove": "aws cloudformation delete-stack --stack-name=StepFunctionStarterPipeline"
    }
}
